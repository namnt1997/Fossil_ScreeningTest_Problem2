<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/color_background_button_fade">
    <item android:id="@android:id/mask">
        <layer-list>
            <item>
                <shape android:shape="oval">
                    <solid android:color="@color/color_background_button_fade" />
                    <stroke
                        android:width="@dimen/all_stroke_button"
                        android:color="@color/color_stroke_button" />
                </shape>
            </item>

            <item android:drawable="@drawable/ic_add" />
        </layer-list>
    </item>

    <item android:id="@android:id/background">
        <layer-list>
            <item>
                <shape android:shape="oval">
                    <solid android:color="@color/color_background_button" />
                    <stroke
                        android:width="@dimen/all_stroke_button"
                        android:color="@color/color_stroke_button" />
                </shape>
            </item>

            <item android:drawable="@drawable/ic_add" />
        </layer-list>
    </item>
</ripple>